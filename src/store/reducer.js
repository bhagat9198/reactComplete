const initialSate = {
  // as have counter in this application. thus careting state with counter property.
  counter: 0
}

const reducer  = (state = initialSate, action) => {
  return state;
}

export default reducer;

// including the reducer within store.