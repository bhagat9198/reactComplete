// we are not writing any JSX, hence not needed.
// import React from 'react';

// children: its a special property that simply outputs whatever gets between the open and closing tag of this component. 
const Auxilliary = (props) => props.children;

export default Auxilliary;